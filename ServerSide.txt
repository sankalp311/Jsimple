select [Column], cast([Value] as varchar(500))
from
    (
        select [TaskId], [Id], [SeqNumber]
        from Schedule
    ) as piv
UNPIVOT
    (  
        [Value]
        for [Column] in ([TaskId], [Id], [SeqNumber])
    ) as unpiv
